---
packages:
  # essentials
  - git
  - curl
  - wget
  - jq
  - htop
  - tree
  - net-tools
  - ca-certificates

  # Python
  - python3
  - python3-pip
  - python3-venv

  # build tools (required by some native npm packages)
  - build-essential

  # dev tooling
  - gh
  - lazygit
  - vim
  - glow

  # Docker
  - docker.io
  - docker-compose-v2
  - docker-buildx

  # Node.js (the nodejs package bundles npm)
  - nodejs
